import React from 'react';

const Navbar = () => (
  <nav className="flex justify-center p-2 bg-blue-800 bg-opacity-50">
    <p className="text-white text-sm">Home | Forecast | About</p>
  </nav>
);

export default Navbar;
